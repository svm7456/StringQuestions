int CharCanConvertinteger(char a){
  char ch=a;
  int p;
  p=(int)ch;
  return p;
}

int main() 
{
  string s;
  cout<<" please enter a string\n";
  getline(cin,s);
  for(int i=0;i<s.length();i++){
      int p=CharCanConvertinteger(s[i]);
      cout<<s[i]<<" "<<p<<"\n";
  }

}
